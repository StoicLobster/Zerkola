{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build Zerkola",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g", "${workspaceFolder}\\src\\main.cpp", "-o", "${workspaceFolder}\\bin\\Zerkola.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}